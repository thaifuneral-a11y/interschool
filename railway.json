{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python"],
      "variables": {
        "NIXPACKS_PYTHON_VERSION": "3.12"
      }
    }
  },
  "deploy": {
    "startCommand": "uvicorn appintersos:app --host 0.0.0.0 --port $PORT"
  }
}
